<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.scalamodules</groupId>
    <artifactId>scalamodules.pom-properties</artifactId>
    <version>1.0.0</version>
    <relativePath>./pom-properties.xml</relativePath>
  </parent>

  <artifactId>scalamodules.pom-license</artifactId>
  <packaging>pom</packaging>

  <build>
    <plugins>
      <plugin>
        <groupId>com.google.code.maven-license-plugin</groupId>
        <artifactId>maven-license-plugin</artifactId>
        <version>1.4.0</version> <!-- Version has to be specified, because maven metadata for that project not up to date :-( -->
        <configuration>
          <header>${basedir}/../../header.txt</header>
          <quiet>false</quiet>
          <failIfMissing>true</failIfMissing>
          <excludes>
            <exclude>.git/**</exclude>
            <exclude>.manager/**</exclude>
          </excludes>
          <useDefaultExcludes>true</useDefaultExcludes>
          <mapping>
            <scala>JAVADOC_STYLE</scala>
          </mapping>
          <useDefaultMapping>true</useDefaultMapping>
          <encoding>UTF-8</encoding>
        </configuration>
        <executions>
          <execution>
            <id>format-headers</id>
            <phase>process-sources</phase>
            <goals>
              <goal>format</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>

</project>
